---
name: status
description: Show Klio daemon state — connection, active space, recent sync.
---

Run the shell command `klio status` and present the JSON output to the user
in a readable summary. Highlight whether `has_creds` is true and which
`socket_path` and `cloud_url` are configured.
